const Logger = require('./singleton_pattern');

Logger.info(`in in info`);

Logger.warning(`wa wa warning`);

const Logger2 = require('./singleton_pattern');


Logger2.info(`in in info`);

Logger2.warning(`wa wa warning`);
